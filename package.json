{
  "name": "456-music",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:mobile": "npm run --prefix apps/mobile start",
    "dev:mobile:tunnel": "npm run --prefix apps/mobile start:tunnel",
    "dev:mobile:clear": "npm run --prefix apps/mobile start:clear",
    "dev:server": "npm run --prefix apps/server dev"
  },
  "description": "Music sharing app monorepo",
  "dependencies": {
    "@expo/metro-runtime": "^55.0.6",
    "expo-dev-client": "~6.0.20"
  }
}
